// Package main — точка входа MAX University Event Bot.
//
// На День 1 — заглушка, компилируется и печатает hello.
// Реальная инициализация (transport + dispatcher + scheduler + admin API)
// появится в Дне 4 (см. execution_plan.md, разделы 13.4 и 4).
package main

import "fmt"

func main() {
	fmt.Println("max-university-event-bot: bot stub (Day 1)")
}
